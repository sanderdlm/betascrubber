# Application Environment
# Set to "dev" for local development (uses local filesystem)
# Set to "production" for production (uses S3/Spaces)
APP_ENV=production

# Temporary Directory (relative to public/ directory or absolute path)
# For dev: use public/tmp so files are web-accessible
# For production: use /tmp since files are uploaded to S3
TMP_DIR=/tmp

# DigitalOcean Spaces Configuration
SPACES_KEY=your_spaces_key
SPACES_SECRET=your_spaces_secret
SPACES_REGION=nyc3
SPACES_BUCKET=splice-frames
SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com
